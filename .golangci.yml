version: "2"
linters:
  default: none
  enable:
    - bodyclose
    - copyloopvar
    - dupl
    - durationcheck
    - errcheck
    - errorlint
    - exhaustive
    - goconst
    - gocritic
    - funlen
    - goprintffuncname
    - gosec
    - gocognit
    - ineffassign
    - makezero
    - misspell
    - nakedret
    - nestif
    - nilerr
    - noctx
    - nolintlint
    - prealloc
    - sqlclosecheck
    - staticcheck
    - tagliatelle
    - testifylint
    - unconvert
    - unparam
    - unused
    - whitespace
  settings:
    funlen:
      lines: 100
      statements: 60
    gocognit:
      min-complexity: 120
    nestif:
      min-complexity: 25
    tagliatelle:
      case:
        rules:
          json: snake
          yaml: snake
    unparam:
      check-exported: true
    staticcheck:
      checks:
        - all
        - "-ST1001" # Allow dot imports in DSL packages
  exclusions:
    generated: strict
    paths:
      - design/*
      - diagram/*
      - gql/*
      - mocks/*
      - codegen/agents/tests/testscenarios/*
    rules:
      - linters:
          - dupl
        path: _test\.go
      - linters:
          - funlen
          - gocognit
          - nestif
        path: _test\.go
      - linters:
          - funlen
          - gocognit
          - nestif
        path: ^codegen/
      - linters:
          - funlen
          - gocognit
          - nestif
        path: ^integration_tests/
