// Code generated by Loom, DO NOT EDIT.
//
// catalog service
//
// Command:
// $ loom gen example.com/assistant/progressive_discovery/design -o
// progressive_discovery

package catalog

import (
	"context"
)

// Minimal MCP progressive-discovery parity fixture
type Service interface {
	// Lookup implements lookup.
	Lookup(context.Context, *LookupPayload) (res *LookupResult, err error)
	// ProjectedLookup implements projected_lookup.
	ProjectedLookup(context.Context, *ProjectedLookupPayload) (res *ProjectedLookupResult, err error)
}

// APIName is the name of the API as defined in the design.
const APIName = "progressive-discovery"

// APIVersion is the version of the API as defined in the design.
const APIVersion = "1.0.0"

// ServiceName is the name of the service as defined in the design. This is the
// same value that is set in the endpoint request contexts under the ServiceKey
// key.
const ServiceName = "catalog"

// MethodNames lists the service method names as defined in the design. These
// are the same values that are set in the endpoint request contexts under the
// MethodKey key.
var MethodNames = [2]string{"lookup", "projected_lookup"}

// LookupPayload is the payload type of the catalog service lookup method.
type LookupPayload struct {
	// Lookup query
	Query string `json:"query"`
}

// LookupResult is the result type of the catalog service lookup method.
type LookupResult struct {
	// Lookup result
	Value string `json:"value"`
}

// ProjectedLookupPayload is the payload type of the catalog service
// projected_lookup method.
type ProjectedLookupPayload struct {
	// Lookup query
	Query string `json:"query"`
}

// ProjectedLookupResult is the result type of the catalog service
// projected_lookup method.
type ProjectedLookupResult struct {
	// Lookup result
	Value string `json:"value"`
}
