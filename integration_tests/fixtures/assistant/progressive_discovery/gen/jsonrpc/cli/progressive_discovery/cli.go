// Code generated by Loom, DO NOT EDIT.
//
// progressive-discovery JSON-RPC client CLI support package
//
// Command:
// $ loom gen example.com/assistant/progressive_discovery/design -o
// progressive_discovery

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	catalogc "example.com/assistant/progressive_discovery/gen/jsonrpc/catalog/client"
	loomhttp "github.com/CaliLuke/loom/http"
	loomhttpcli "github.com/CaliLuke/loom/http/cli"
	loom "github.com/CaliLuke/loom/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{"catalog (lookup|projected-lookup)"}
} // UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " catalog lookup --body '{\n      \"query\": \"Est rerum odio omnis ut reprehenderit.\"\n   }'\\n"
}

type commandLine struct {
	Catalog struct {
		Lookup struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Lookup implements lookup." name:"lookup"`
		ProjectedLookup struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"ProjectedLookup implements projected_lookup." name:"projected-lookup"`
	} `cmd:"" help:"Minimal MCP progressive-discovery parity fixture" name:"catalog"`
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(scheme string, host string, doer loomhttp.Doer, enc func(*http.Request) loomhttp.Encoder, dec func(*http.Response) loomhttp.Decoder, restore bool) (loom.Endpoint, any, error) {
	var (
		command                        commandLine
		args                           []string
		svcn                           string
		epn                            string
		catalogLookupBodyFlag          *string
		catalogProjectedLookupBodyFlag *string
	)
	{
		args = flag.Args()
		if len(args) == 0 {
			args = os.Args[1:]
		}
		path, err := loomhttpcli.Parse(&command, os.Args[0], args)
		if err != nil {
			return nil, nil, fmt.Errorf("parse command: %w", err)
		}
		catalogLookupBodyFlag = &command.Catalog.Lookup.Body
		catalogProjectedLookupBodyFlag = &command.Catalog.ProjectedLookup.Body
		switch path {
		case "catalog lookup":
			svcn = "catalog"
			epn = "lookup"
		case "catalog projected-lookup":
			svcn = "catalog"
			epn = "projected-lookup"
		}
	}

	var (
		data     any
		endpoint loom.Endpoint
		err      error
	)
	{
		switch svcn {
		case "catalog":
			c := catalogc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "lookup":
				endpoint = c.Lookup()
				data, err = catalogc.BuildLookupPayload(*catalogLookupBodyFlag)
			case "projected-lookup":
				endpoint = c.ProjectedLookup()
				data, err = catalogc.BuildProjectedLookupPayload(*catalogProjectedLookupBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}
	return endpoint, data, nil
} // catalogUsage displays the usage of the catalog command and its subcommands.
func catalogUsage() {
	fmt.Fprintln(os.Stderr, "Minimal MCP progressive-discovery parity fixture")
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] catalog COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, "    lookup: Lookup implements lookup.")
	fmt.Fprintln(os.Stderr, "    projected-lookup: ProjectedLookup implements projected_lookup.")
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s catalog COMMAND --help\n", os.Args[0])
}
func catalogLookupUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] catalog lookup", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Lookup implements lookup.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "catalog lookup --body '{\n      \"query\": \"Est rerum odio omnis ut reprehenderit.\"\n   }'")
}
func catalogProjectedLookupUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] catalog projected-lookup", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "ProjectedLookup implements projected_lookup.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "catalog projected-lookup --body '{\n      \"query\": \"Molestias quae voluptatem fugit.\"\n   }'")
}
