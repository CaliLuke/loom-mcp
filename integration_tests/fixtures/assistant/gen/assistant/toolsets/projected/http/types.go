// Code generated by Loom, DO NOT EDIT.
//
// projected tool transport types
//
// Command:
// $ loom gen example.com/assistant/design

package http

type (
	// ProjectedBoundedLookupToolPayloadTransport is the internal JSON transport type for ProjectedBoundedLookupToolPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ProjectedBoundedLookupToolPayloadTransport struct {
		// Projected lookup query
		Query *string `json:"query"`
		// Opaque continuation cursor
		Cursor *string `json:"cursor,omitempty"`
		// Verified server-owned session identifier
		SessionID *string `json:"-"`
	}

	// ProjectedBoundedLookupToolResultTransport is the internal JSON transport type for ProjectedBoundedLookupToolResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ProjectedBoundedLookupToolResultTransport struct {
		// Bounded lookup hits
		Hits []string `json:"hits"`
	}

	// ProjectedLookupToolPayloadTransport is the internal JSON transport type for ProjectedLookupToolPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ProjectedLookupToolPayloadTransport struct {
		// Projected lookup query
		Query *string `json:"query"`
	}

	// ProjectedLookupToolResultTransport is the internal JSON transport type for ProjectedLookupToolResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ProjectedLookupToolResultTransport struct {
		// Projected lookup answer
		Answer *string `json:"answer"`
		// Projected lookup source
		Source *string `json:"source"`
	}

	// ProjectedStatusToolPayloadTransport is the internal JSON transport type for ProjectedStatusToolPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ProjectedStatusToolPayloadTransport struct {
	}

	// ProjectedStatusToolResultTransport is the internal JSON transport type for ProjectedStatusToolResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ProjectedStatusToolResultTransport struct {
		// Projected runtime status
		Status *string `json:"status"`
	}
)
