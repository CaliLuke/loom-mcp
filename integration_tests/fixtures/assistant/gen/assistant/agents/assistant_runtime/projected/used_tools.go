// Code generated by Loom, DO NOT EDIT.
//
// projected used tool helpers
//
// Command:
// $ loom gen example.com/assistant/design

package projected

import (
	projectedspecs "example.com/assistant/gen/assistant/toolsets/projected"
	"github.com/CaliLuke/loom-mcp/v2/runtime/agent/planner"
	"github.com/CaliLuke/loom-mcp/v2/runtime/agent/tools"
)

// Used toolset typed helpers for projected
// These helpers mirror the agent-as-tool helpers to provide a consistent planner UX.
// They expose typed payload/result aliases and `New<Tool>Call` builders.

// Tool IDs (globally unique). Use these constants in planner tool calls.
const (
	ProjectedLookupTool tools.Ident = "projected.projected_lookup_tool"
	ProjectedStatusTool tools.Ident = "projected.projected_status_tool"
)

// Type aliases and codec re-exports for convenience.
type ProjectedLookupToolPayload = projectedspecs.ProjectedLookupToolPayload

var ProjectedLookupToolPayloadCodec = projectedspecs.ProjectedLookupToolPayloadCodec

type ProjectedLookupToolResult = projectedspecs.ProjectedLookupToolResult

var ProjectedLookupToolResultCodec = projectedspecs.ProjectedLookupToolResultCodec

type ProjectedStatusToolPayload = projectedspecs.ProjectedStatusToolPayload

var ProjectedStatusToolPayloadCodec = projectedspecs.ProjectedStatusToolPayloadCodec

type ProjectedStatusToolResult = projectedspecs.ProjectedStatusToolResult

var ProjectedStatusToolResultCodec = projectedspecs.ProjectedStatusToolResultCodec

// CallOption customizes planner.ToolRequest values built by the typed helpers.
type CallOption func(*planner.ToolRequest)

// WithParentToolCallID sets the ParentToolCallID on the constructed request.
func WithParentToolCallID(id string) CallOption {
	return func(r *planner.ToolRequest) {
		r.ParentToolCallID = id
	}
}

// WithToolCallID sets a model/tool-call identifier on the request. The runtime
// preserves this ID and echoes it in ToolResult.ToolCallID for correlation.
func WithToolCallID(id string) CallOption {
	return func(r *planner.ToolRequest) {
		r.ToolCallID = id
	}
}

// Typed tool-call helpers (one per tool). These ensure use of the generated tool ID
// and accept typed payloads matching tool schemas.
// NewProjectedLookupToolCall builds a planner.ToolRequest for
// projected.projected_lookup_tool.
func NewProjectedLookupToolCall(args *ProjectedLookupToolPayload, opts ...CallOption) planner.ToolRequest {
	var payload []byte
	var toolErr *planner.ToolError
	if args != nil {
		// Encode typed payloads into canonical JSON using the generated codec.
		b, err := ProjectedLookupToolPayloadCodec.ToJSON(args)
		if err != nil {
			toolErr = planner.ToolErrorFromError(err)
		} else {
			payload = b
		}
	}
	req := planner.ToolRequest{
		Error:   toolErr,
		Name:    ProjectedLookupTool,
		Payload: payload,
	}
	for _, o := range opts {
		if o != nil {
			o(&req)
		}
	}
	return req
}

// NewProjectedStatusToolCall builds a planner.ToolRequest for
// projected.projected_status_tool.
func NewProjectedStatusToolCall(args *ProjectedStatusToolPayload, opts ...CallOption) planner.ToolRequest {
	var payload []byte
	var toolErr *planner.ToolError
	if args != nil {
		// Encode typed payloads into canonical JSON using the generated codec.
		b, err := ProjectedStatusToolPayloadCodec.ToJSON(args)
		if err != nil {
			toolErr = planner.ToolErrorFromError(err)
		} else {
			payload = b
		}
	}
	req := planner.ToolRequest{
		Error:   toolErr,
		Name:    ProjectedStatusTool,
		Payload: payload,
	}
	for _, o := range opts {
		if o != nil {
			o(&req)
		}
	}
	return req
}
