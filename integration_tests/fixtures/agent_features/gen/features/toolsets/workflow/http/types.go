// Code generated by Loom, DO NOT EDIT.
//
// workflow tool transport types
//
// Command:
// $ loom gen example.com/agentfeatures/design

package http

type (
	// DraftPayloadTransport is the internal JSON transport type for DraftPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	DraftPayloadTransport struct {
		// Topic to process
		Topic *string `json:"topic"`
	}

	// DraftResultTransport is the internal JSON transport type for DraftResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	DraftResultTransport struct {
		// Whether the operation succeeded
		OK *bool `json:"ok"`
		// Whether the operation was approved
		Approved *bool `json:"approved,omitempty"`
	}

	// MethodEchoPayloadTransport is the internal JSON transport type for MethodEchoPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	MethodEchoPayloadTransport struct {
		// Topic to send to the service method
		Topic *string `json:"topic"`
	}

	// MethodEchoResultTransport is the internal JSON transport type for MethodEchoResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	MethodEchoResultTransport struct {
		// Whether the method-backed tool succeeded
		OK *bool `json:"ok"`
		// Message returned to the agent runtime
		Message *string `json:"message"`
	}

	// PublishPayloadTransport is the internal JSON transport type for PublishPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	PublishPayloadTransport struct {
	}

	// PublishResultTransport is the internal JSON transport type for PublishResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	PublishResultTransport struct {
		// Whether the operation succeeded
		OK *bool `json:"ok"`
		// Whether the operation was approved
		Approved *bool `json:"approved,omitempty"`
	}

	// RetryPayloadTransport is the internal JSON transport type for RetryPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	RetryPayloadTransport struct {
	}

	// RetryResultTransport is the internal JSON transport type for RetryResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	RetryResultTransport struct {
		// Whether the operation succeeded
		OK *bool `json:"ok"`
		// Whether the operation was approved
		Approved *bool `json:"approved,omitempty"`
	}

	// ReviewPayloadTransport is the internal JSON transport type for ReviewPayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReviewPayloadTransport struct {
		// Whether review should be strict
		Strict *bool `json:"strict"`
	}

	// ReviewResultTransport is the internal JSON transport type for ReviewResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReviewResultTransport struct {
		// Whether the operation succeeded
		OK *bool `json:"ok"`
		// Whether the operation was approved
		Approved *bool `json:"approved,omitempty"`
	}

	// RevisePayloadTransport is the internal JSON transport type for RevisePayload.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	RevisePayloadTransport struct {
	}

	// ReviseResultTransport is the internal JSON transport type for ReviseResult.
	// It lives in the toolset-local http package and is used only for JSON
	// decode + validation (missing-field detection) before transforming into
	// the public tool type.
	ReviseResultTransport struct {
		// Whether the operation succeeded
		OK *bool `json:"ok"`
		// Whether the operation was approved
		Approved *bool `json:"approved,omitempty"`
	}
)
