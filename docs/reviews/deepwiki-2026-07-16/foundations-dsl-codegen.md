# DeepWiki review: foundations, DSL, and code generation

Reviewed 2026-07-16. DeepWiki is indexed at commit `dd8379472f8341a694e65ce53935fe6cda12c2ad` (shown by the site as `dd8379` on every page). This review covers pages 1 through 3.2. Code and generated fixture output are authoritative where they conflict with prose.

Classification: `DOC-GAP` means a local user-facing document is wrong or incomplete; `SKILL-GAP` means the routed loom-mcp skill is wrong or incomplete; `DEEPWIKI-INACCURACY` means the external generated wiki is wrong; `ARCH-IMPROVEMENT` is an implementation or maintainability recommendation. Priority is P1 (unsafe/high impact) through P3 (low impact).

## Page results

| DeepWiki page | Result | Evidence |
| --- | --- | --- |
| [1. Overview](https://deepwiki.com/CaliLuke/loom-mcp/1-overview) | Mostly accurate architecture overview; its `memory.Store` entry still conflates transcript events, runlog, and long-term memory. | `runtime/agent/memory`, `runtime/agent/runlog`, and the long-term `memory.Service` are separate contracts; `.agents/skills/loom-mcp/SKILL.md` explicitly distinguishes them. |
| [1.1. Getting Started & Quickstart](https://deepwiki.com/CaliLuke/loom-mcp/1.1-getting-started-and-quickstart) | Current Go requirement and optional Temporal guidance match local docs. The generated-layout description remains stale. | `go.mod:3`, `quickstart/README.md`, and `codegen/agent/generate_agent_files.go:13-52`. |
| [1.2. Repository Structure & Conventions](https://deepwiki.com/CaliLuke/loom-mcp/1.2-repository-structure-and-conventions) | Matches current repository layout and local/remote Loom workflow. | `README.md`, `AGENTS.md`, `scripts/loom_core_mode.sh`. |
| [2. DSL & Design Layer](https://deepwiki.com/CaliLuke/loom-mcp/2-dsl-and-design-layer) | The expression-root model is accurate; timing language is unsafe. | `docs/dsl.md:1428-1440`; `runtime/agent/runtime/workflow_loop.go`. |
| [2.1. Agent & Toolset DSL](https://deepwiki.com/CaliLuke/loom-mcp/2.1-agent-and-toolset-dsl) | Composition is accurate; it gives invalid projection and obsolete policy guidance. | `expr/agent/tool.go:399-468`, `dsl/history.go`, `dsl/idempotency.go`. |
| [2.2. MCP Service DSL](https://deepwiki.com/CaliLuke/loom-mcp/2.2-mcp-service-dsl) | Broad surface is accurate; projected-tool and OAuth examples omit essential constraints. | `docs/dsl.md:911-942,471-488`; `dsl/mcp.go`. |
| [2.3. Execution Policy & Workflow DSL](https://deepwiki.com/CaliLuke/loom-mcp/2.3-execution-policy-and-workflow-dsl) | Workflow-node coverage is mostly good; timing, idempotency, compression, and graph-mode claims are stale. | `dsl/workflow.go`, `dsl/timing.go`, `runtime/agent/runtime/workflow_await_queue.go`. |
| [3. Code Generation](https://deepwiki.com/CaliLuke/loom-mcp/3-code-generation) | The two-generator overview is accurate; emitted agent artifacts and schema terminology are wrong. | `codegen/agent/generate.go:34-86`, `codegen/agent/generate_agent_files.go:13-52`. |
| [3.1. Agent Code Generation](https://deepwiki.com/CaliLuke/loom-mcp/3.1-agent-code-generation) | Type, codec, injection, and transform descriptions are accurate; artifact table is stale. | `integration_tests/fixtures/assistant/gen/assistant/agents/assistant_runtime/`; `.../toolsets/projected/`. |
| [3.2. MCP Code Generation](https://deepwiki.com/CaliLuke/loom-mcp/3.2-mcp-code-generation) | Adapter/SDK architecture is accurate; it overstates OAuth enforcement and misstates local-provider construction. | `docs/mcp_sdk_server.md`, `codegen/mcp/local_provider_file.go`, generated `local_provider.go`. |

## Discrepancy log

| ID | Class / priority | DeepWiki page and discrepancy | Repository evidence | Recommended action |
| --- | --- | --- | --- | --- |
| FDC-01 | `DOC-GAP` P1 | Pages 1.1, 3, and 3.1 say every agent emits `workflow.go` (and page 3 also describes per-agent toolset specs). `docs/dsl.md:1786-1817` repeats the same false file list. | `codegen/agent/generate_agent_files.go:13-52` emits `agent.go`, `config.go`, `registry.go`, aggregate specs/catalog, and conditional helper packages. `integration_tests/fixtures/assistant/gen/assistant/agents/assistant_runtime/` contains no `workflow.go` or `activities.go`; defining types/codecs/specs are owner-scoped at `gen/<service>/toolsets/<toolset>/`. | Correct the generated-artifacts section in `docs/dsl.md`; add a generator documentation test that compares documented fixed files with `agentFiles` output. Regenerate or request a DeepWiki refresh afterwards. |
| FDC-02 | `ARCH-IMPROVEMENT` P2 | The false generated-file story is also embedded in `RuntimeData` GoDoc, which DeepWiki treats as source evidence. | `codegen/agent/data.go:709-819` repeatedly refers to `workflow.go.tpl`/`activities.go.tpl`; actual runtime metadata is consumed by `agent_jennifer.go` and `registry_render.go`, not emitted as those files. | Rewrite the GoDoc to describe registration names/options rather than nonexistent emitted handlers; remove stale template references. Keep a focused data-model test, not an output-layout claim, for this metadata. |
| FDC-03 | `DEEPWIKI-INACCURACY` P1 | Pages 2, 2.1, and 2.3 call `TimeBudget` a wall-clock or hard run timeout. | Active-time deadlines are extended for clarification, confirmation, typed-input, and external-tool waits; the local contract says this explicitly in `docs/dsl.md:1428-1440` and `.agents/skills/loom-mcp/SKILL.md`. | Refresh/correct DeepWiki. Keep caller/workflow-boundary SLA guidance in public docs and the skill; no product change is warranted. |
| FDC-04 | `DEEPWIKI-INACCURACY` P1 | Page 2.1 teaches `Expose(MCPSurface)` for projected toolsets without `AgentRuntime` and placement constraints. | `expr/agent/tool.go:399-468` requires a method-backed tool, `Expose(AgentRuntime, MCPSurface)`, and same-service `MCPPlacement`; several v1 features are rejected. | Correct the external example before users copy it. Local docs and skill contract are already explicit. |
| FDC-05 | `DEEPWIKI-INACCURACY` P1 | Page 2.3 says `Idempotent()` can cache/reuse calls; it also advertises nonexistent `Compress()`. | `dsl/idempotency.go` only emits metadata; `dsl/history.go` provides `CompressAtTurns` / `CompressAtMaxInputTokens`. The local skill correctly calls idempotency metadata-only. | Correct the external page. Preserve the current explicit metadata-only contract unless a separately designed replay-suppression feature is approved. |
| FDC-06 | `DEEPWIKI-INACCURACY` P2 | Pages 3 and 3.1 describe model tool schemas as OpenAPI schemas. | Generated `ToolSpec` payloads state JSON Schema draft 2020-12, e.g. `integration_tests/fixtures/assistant/gen/assistant/toolsets/projected/specs.go:25-35`. Goa's OpenAPI machinery is an implementation detail. | Use “JSON Schema draft 2020-12” for the emitted model contract; retain an implementation-note only where relevant. |
| FDC-07 | `DEEPWIKI-INACCURACY` P1 | Page 3.2 implies that OAuth DSL wiring installs authorization/challenge enforcement and calls Protected Resource Metadata “RFC 9470/9728.” | The DSL emits metadata/discovery helpers. Applications install credential verification and `runtime/mcp.WithOAuthChallenge`; `docs/dsl.md:471-488` documents that boundary. The implementation uses RFC 9728 PRM and RFC 6750 challenge behavior. | Correct DeepWiki to name the application-owned authentication boundary and supported RFCs. Do not make generated middleware silently authenticate requests. |
| FDC-08 | `DEEPWIKI-INACCURACY` P2 | Page 3.2 calls local provider a convenience constructor that instantiates service, adapter, and server. | `New<Service><MCP>LocalToolsetRegistration(adapter)` takes an existing adapter and returns an in-process `runtime.ToolsetRegistration`; see `codegen/mcp/local_provider_file.go` and generated `local_provider.go`. | Correct the external description; retain the explicit adapter dependency, which keeps transport initialization out of local agent execution. |

## Documentation and skill assessment

The current loom-mcp skill and its codegen-layout/reference guides correctly route agents to the owner-scoped generated surface. No new `SKILL-GAP` was found in this scope. The remaining local user-facing inconsistency is FDC-01, and its stale generator GoDoc is FDC-02. Those source comments are particularly important because DeepWiki cites them and turns them into user-facing claims.

## Recommended order

1. Fix FDC-01 and FDC-02 together, with an assertion that prevents the generated-artifact narrative from drifting again.
2. Refresh DeepWiki after the local source correction; include FDC-03 through FDC-08 in the edit/feedback request because they remain unsafe copy guidance despite local docs and skill being correct.
3. Do not add runtime workarounds for DeepWiki statements. The existing contracts—active-time budgets, metadata-only idempotency, explicit OAuth middleware, and owner-scoped specs—are coherent.
